import { IndustrypipePipe } from './industrypipe.pipe';

describe('IndustrypipePipe', () => {
  it('create an instance', () => {
    const pipe = new IndustrypipePipe();
    expect(pipe).toBeTruthy();
  });
});
