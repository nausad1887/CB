import { DegreepipePipe } from './degreepipe.pipe';

describe('DegreepipePipe', () => {
  it('create an instance', () => {
    const pipe = new DegreepipePipe();
    expect(pipe).toBeTruthy();
  });
});
