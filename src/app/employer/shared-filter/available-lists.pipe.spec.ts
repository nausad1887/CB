import { AvailableListsPipe } from './available-lists.pipe';

describe('AvailableListsPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableListsPipe();
    expect(pipe).toBeTruthy();
  });
});
