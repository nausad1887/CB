import { EmperiancePipe } from './emperiance.pipe';

describe('EmperiancePipe', () => {
  it('create an instance', () => {
    const pipe = new EmperiancePipe();
    expect(pipe).toBeTruthy();
  });
});
